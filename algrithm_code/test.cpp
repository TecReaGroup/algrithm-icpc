#include <bits/stdc++.h>
using namespace std;

int main(){
    vector<int> nums = {1, 2, 3, 4, 5};
    vector<int> ans(nums.begin(), nums.end());
    for (int i = 0; i < ans.size(); i++){
        cout << ans[i] << " ";
    }

}